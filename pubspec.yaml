name: freelance_time_controller_api
description: A server app using the shelf package and Docker.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.16.2 <3.0.0'


dependencies:
  args: ^2.0.0
  dotenv: ^4.0.1
  dart_dotenv: ^1.0.1
  mongo_dart: ^0.7.4+1
  shelf: ^1.1.0
  shelf_router: ^1.0.0
  shelf_router_generator: ^1.0.2
  
  

dev_dependencies:
  http: ^0.13.0
  lints: ^1.0.0
  test_process: ^2.0.0
  build_runner: ^2.1.10
  test: ^1.15.0
